apiVersion: v1
kind: Service
metadata:
  name: {{ include "emho-helm.fullname" . }}
  labels:
    {{- include "emho-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.externalPort }}
      targetPort: {{ .Values.service.internalPort }}
      nodePort: {{ .Values.service.nodePort}}
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    {{- include "emho-helm.selectorLabels" . | nindent 4 }}
